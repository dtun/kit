{
	"name": "kit",
	"version": "0.1.0",
	"private": true,
	"description": "Kit — Kinetic Intelligence Tool. Family AI assistant on Cloudflare.",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"test": "vitest run --config vitest.unit.config.ts",
		"test:watch": "vitest --config vitest.unit.config.ts",
		"test:domain": "vitest run --config vitest.unit.config.ts test/domain",
		"test:app": "vitest run --config vitest.unit.config.ts test/application",
		"test:adapters": "vitest run --config vitest.unit.config.ts test/adapters",
		"test:integration": "vitest run --config vitest.integration.config.ts",
		"test:all": "npm test && npm run test:integration",
		"eval:dev": "evalite watch",
		"eval:run": "evalite run",
		"eval:ci": "evalite run --threshold 80",
		"check": "biome check .",
		"check:fix": "biome check --fix .",
		"typecheck": "tsc --noEmit",
		"tail": "wrangler tail"
	},
	"dependencies": {
		"agents": "^0.0.98",
		"hono": "^4.7.0",
		"postal-mime": "^2.7.4",
		"zod": "^3.25.76"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@cloudflare/vitest-pool-workers": "^0.8.0",
		"@cloudflare/workers-types": "^4.20250327.0",
		"evalite": "^0.19.0",
		"typescript": "^5.7.3",
		"vite-tsconfig-paths": "^5.0.0",
		"vitest": "^3.1.0",
		"wrangler": "^4.14.0"
	}
}
