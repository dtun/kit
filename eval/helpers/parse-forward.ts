// Re-export the pure parser so eval suites have a stable entry point
// alongside classify/process-message helpers.
export { parseForwardedEmail } from "@application/use-cases/parse-forwarded-email";
